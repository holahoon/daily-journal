import Authentication from "components/authentication/Authentication";

function Navigation() {
  return (
    <header>
      <nav>
        <div>logo</div>
        <Authentication />
      </nav>
    </header>
  );
}

export default Navigation;
